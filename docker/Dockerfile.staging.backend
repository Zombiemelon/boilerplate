FROM composer:1.7.2 as build-stage
COPY ./dvmeal_backend /app
RUN composer install

FROM php:7.3-fpm
ARG arg
ENV staging=$arg
RUN apt-get update; \
    apt-get install -y libpng-dev; \
    docker-php-ext-install gd \
    pdo \
    pdo_mysql
RUN apt-get -y install nginx \
    supervisor
COPY ./docker/nginx/sites-enabled-back /etc/nginx/conf.d/default.conf
COPY --from=build-stage /app /home/dvmeal/dvmeal_backend
COPY ./docker/nginx/nginx.conf /etc/nginx/nginx.conf
COPY ./docker/supervisor/supervisord.conf /etc/supervisor/supervisord.conf

RUN chown -R :www-data /home/dvmeal/dvmeal_backend/storage/logs/; \
    chown -R :www-data /home/dvmeal/dvmeal_backend/storage/; \
    chown -R :www-data /home/dvmeal/dvmeal_backend/public; \
    chown -R :www-data /home/dvmeal/dvmeal_backend/tests; \
    chmod -R g+w /home/dvmeal/dvmeal_backend/storage; \
    chmod -R g+w /home/dvmeal/dvmeal_backend/public; \
    chmod -R 777 /home/dvmeal/dvmeal_backend/tests; \
    chmod -R 777 /home/dvmeal/dvmeal_backend/migration.sh;
RUN mkdir /home/dvmeal/dvmeal_backend/tests/functional
RUN cd /home/dvmeal/dvmeal_backend; \
    cp $staging .env; \
    php artisan key:generate

EXPOSE 80

CMD ["service", "supervisor", "start"]